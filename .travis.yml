language: node_js
node_js:
  - "8"
cache:
  directories:
  - node_modules
script:
  - npm test
  - npm run build
deploy:
  provider: pages
  skip_cleanup: true
  target-branch: master
  github_token: $github_token
  local_dir: dist
  on:
    branch: develop
